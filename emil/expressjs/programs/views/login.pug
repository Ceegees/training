html
 head
  title Signup
 body
  if(message)
  h4 #{messsage}
  form(action = "/login" method = "POST")
   input(name = "id" type = "text" required placeholder = "Usre Id" )
   input(name = "password" type = "password" required placeholder = "password")
   button(type = "Submit") Log in